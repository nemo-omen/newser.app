# Supa Repo

We're using Supabase for this repo, but we're probably not going to be using the `supa` client for anything other than auth. Most of the functions defined here will (probably) use KSQL.

